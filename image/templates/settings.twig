{% import "_includes/forms" as forms %}
<h2>Images Directory</h2>
{{ forms.textField({
  label: "Enter the directory path for your images. If one isn't supplied we will attempt to use one defined in your 'environmentVariables' in your [General Config][1] file.
  [1]: https://craftcms.com/docs/config-settings"|markdown,
  id: 'imageDirectory',
  name: 'imageDirectory',
  placeholder: 'Default: /assets/images',
  value: settings.imageDirectory,
  errors: settings.getErrors('imageDirectory'),
}) }}
